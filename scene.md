# 场景列表
## 新增特征
- 用户功能扩展
  - 用户首次使用（本地无记录），提醒用户生成或输入钱包 ID（等同于 bit 钱包 id）, 并提醒用户记录私钥，公钥，钱包 ID
  - 钱包 ID，固化本项目生成 ID 的配置
  - 自动生成用户名；用户可以修改
- 任务管理
  - 原有的取件码仍然有效
  - 任务记录日志
  - 推送
    - 向已有联系人/群组推送，对方端点链接上来后，自动下载

- 联系人扩展
  - 记录用户不同的端点为联系人（同一用户，自动识别用户的设备：windows-xxx,mac-xxx,iphone-xxx, android-xxx, linux-xxx）
  - 记录用户最近协作过的联系人（持有其公钥、钱包地址、设备标签）
- 组管理
  - 将多个联系人拉入群组、提出群组
- 项目名： ShareYouSee

### 调整细节
  - 顶部“FastSend", 主标题和副标题都取消，节省面板空间
  - 快速开始下的发送按钮组
    - 缩小按钮，并设置为一行
    - 上方增加两个 tab， tab 内可以滚动
      - 最近协作（我的端点/联系人/群组）
      - 联系人（展示我的端点，联系人，群组）
        - 我的端点
        - 其他联系人不显示端点
        - 可选中
          - 定向发送；如果不选中人安取件码逻辑操作
          - 加入群组，弹出群组 CRUD 窗口，进行操作（群组不能加入群组）
  - 取件码（正常保留）
### 其他修正
  - 定向任务，用户端可以拒绝（因为用户可能只想在一个设备接受，其他设备可拒绝）

1）用户打开网页，拉去定向推送的任务
2）用户点击通知按钮，弹出任务框，用户一次接受
3）发送者选择联系人/或者置空，选择文件/文件夹/同步文件夹
  - 发送任务，获得 code （以前流程，额外增加了可选参数目标端点（array<{walletId+label?}>）
    - 600 秒缓存任务
    - 完成一个删除一个array<{walletId+label?}>元素，全部完成删除 code 
  - 目标用户，通过 code  接受任务；通过定向任务通知接受任务
  - 完成通知

## 发送端

- 选择文件或目录
- 初始阶段
  - 连接信令服务器
    - 连接失败（报错
  - 获取收件码
    - 当前连接人数太多，稍后再试（报错 -1
- 等待接入
  - 信令连接断开或超时（报错
  - 对方不支持传输目录（现代文件访问API（报错
  - 用户确认是否传输
- 开始传输
  - 与对等端断开（报错
- 完成传输任务

## 接收端

- 初始阶段
  - 连接信令服务器
    - 失败报错
    - 找不到对等端（报错 404
  - 连接对等端
    - 超时报错
  - 传输基本信息（用户，类型，文件
    - 不支持目录传输（报错
  - 展示信息，等待用户继续
    - 同步目录
      - 等待用户选择目录
      - 对比差异
      - 等待用户选择文件（新增、更新、删除
      - 等待继续
- 传输阶段
  - 对等端断开（报错
- 传输完成
  - 展示本次传输信息
  - 展示回首页等按钮
